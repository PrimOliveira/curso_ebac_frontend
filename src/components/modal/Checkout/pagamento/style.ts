import styled from "styled-components"
import { CheckoutContainer } from "../style";
import * as Entrega from "../entrega/style";

export const PagamentoContainer = styled(CheckoutContainer)``;

export const CheckoutForm = styled.form`
    width: 100%;
    height: 450px;
`
export const Row = styled(Entrega.Row)``;

export const InputGroup = styled(Entrega.InputGroup)``;

export const BotaoGroup = styled(Entrega.BotaoGroup)``;